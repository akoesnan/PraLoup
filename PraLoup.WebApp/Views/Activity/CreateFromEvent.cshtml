@model PraLoup.WebApp.Models.ActivityModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Activity</legend>
        
        @Html.HiddenFor(model => model.Activity.Event.EventId)
        <div class="editor-label">
            @Html.LabelFor(model => model.Activity.Privacy)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Activity.Privacy)
            @Html.ValidationMessageFor(model => model.Activity.Privacy)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
